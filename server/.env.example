# Project URL (Settings > API) — base only, no trailing /rest/v1
SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_SERVICE_ROLE_KEY=<service-role-key>

# Shared secret for the admin routes (x-admin-secret / Bearer). Unset disables them.
ADMIN_SECRET=<random-long-string>
